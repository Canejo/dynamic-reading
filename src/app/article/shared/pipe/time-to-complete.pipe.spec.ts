import { TimeToCompletePipe } from './time-to-complete.pipe';

describe('TimeToCompletePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeToCompletePipe();
    expect(pipe).toBeTruthy();
  });
});
